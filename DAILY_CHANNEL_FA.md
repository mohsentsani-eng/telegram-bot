# پیام روزانه کانال ترنم همدلی

ربات هر روز یک پیام کوتاه آموزشی/روان‌شناختی مرتبط با مطالعه در کانال منتشر می‌کند.

- ساعت پیش‌فرض: 09:00 به وقت محلی سیستم
- `DAILY_MESSAGE_HOUR` برای تغییر ساعت
- `DAILY_MESSAGE_MINUTE` برای تغییر دقیقه
- بات باید در کانال `REQUIRED_CHANNEL_ID` دسترسی ارسال پیام داشته باشد.
- پیام‌ها در `app/bot.py` در فهرست `DAILY_MESSAGES` قرار دارند و از `data/daily_messages.json` نیز به‌عنوان بانک قابل ویرایش استفاده شده است.
- ربات برای هر روز فقط یک پیام منتشر می‌کند و تاریخ آخرین انتشار را در `data/daily_message_state.json` نگه می‌دارد.
