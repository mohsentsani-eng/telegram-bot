# فعال‌سازی Gemini در بات ترنم همدلی

1. از Google AI Studio یک API Key بسازید.
2. فایل `.env.example` را در کنار `main.py` کپی و نام آن را `.env` کنید.
3. مقدار `GEMINI_API_KEY` را فقط داخل `.env` قرار دهید.
4. مقدار `AI_ENABLED=true` باشد.
5. بات را از همان پوشه اصلی پروژه اجرا کنید.

نمونه:

GEMINI_API_KEY=YOUR_KEY
GEMINI_MODEL=gemini-3.8-flash
AI_ENABLED=true

کلید API را داخل کد، ZIP یا پیام تلگرام قرار ندهید.

در صورت فعال بودن، بخش دستیار هوشمند از کلید موجود در محیط استفاده می‌کند.


## خطای 503
اگر Gemini در یک لحظه با 503/UNAVAILABLE پاسخ دهد، بات به‌صورت خودکار چند بار با فاصله افزایشی تلاش می‌کند و سپس مدل‌های جایگزین تنظیم‌شده را امتحان می‌کند. این خطا معمولاً موقتی و ناشی از بار سرویس است.

## قابلیت جدید: ارسال داده برای تحلیل
در منوی «🤖 دستیار هوشمند»، گزینه «📥 ارسال داده برای تحلیل» اضافه شده است. دانش‌آموز می‌تواند نتیجه کنکور، کارنامه آزمون، نتایج تست یا توضیح آموزشی خود را به صورت متن ارسال کند و Gemini فقط همان داده را تحلیل می‌کند و از اطلاعات پرونده برای شخصی‌سازی استفاده می‌کند.
