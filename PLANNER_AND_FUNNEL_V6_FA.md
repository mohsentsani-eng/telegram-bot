# نسخه V6 — قیف عضویت کانال و برنامه‌ریزی تخصصی

## قیف کانال
- عضویت کانال با `@tarnoomhamdeli` به صورت پیش‌فرض فعال است.
- بعد از ارزیابی سریع، اگر کاربر عضو باشد دعوت مجدد نشان داده نمی‌شود.
- اگر عضو نباشد، دکمه عضویت و «عضو شدم» نمایش داده می‌شود.
- خدمات تحصیلی/روان‌شناختی/مهارت یادگیری/انتخاب رشته/دستیار هوشمند همچنان از `require_channel` استفاده می‌کنند.
- منوی اصلی یک ورودی مستقیم «📢 کانال ترنم همدلی» دارد.

## برنامه‌ریزی تخصصی
برنامه‌ریزی دیگر یک پیام ساده نیست. ربات ۷ ورودی می‌گیرد:
1. حداکثر ساعت مطالعه مفید روزانه
2. ظرفیت مطالعه صبح
3. ظرفیت مطالعه ظهر تا شب
4. برنامه مدرسه با روز و ساعت
5. کلاس‌های فوق‌برنامه با روز و ساعت
6. برنامه آزمون‌ها
7. درس‌ها/هدف‌های اولویت‌دار

سپس یک برنامه اولیه هفتگی تولید و در جدول `plans` ذخیره می‌شود. ساعات مدرسه و کلاس‌ها از پنجره‌های مطالعه کم می‌شوند، و ظرفیت صبح/عصر رعایت می‌شود.

## ادامه مسیر مشاور
بعد از تولید برنامه اولیه:
- اگر کاربر عضو کانال باشد: دکمه «درخواست اصلاح برنامه توسط مشاور» نمایش داده می‌شود.
- اگر عضو نباشد: ابتدا دعوت به کانال نمایش داده می‌شود و بعد از تأیید عضویت امکان درخواست اصلاح برنامه فعال می‌شود.
- درخواست در CRM در جدول `counseling_requests` با نوع `plan_review` ذخیره می‌شود.

## رفع نمایش خام HTML
Bot با `DefaultBotProperties(parse_mode=HTML)` ساخته شده تا تگ‌هایی مثل `<b>` به صورت متن خام نمایش داده نشوند.
