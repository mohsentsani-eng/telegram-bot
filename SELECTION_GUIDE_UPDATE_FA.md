# راهنمای به‌روزرسانی انتخاب رشته ۱۴۰۵

## وضعیت فعلی
- سال هدف سامانه: **۱۴۰۵**
- سال فعال برای تحلیل رشته‌محل و ضوابط: **۱۴۰۴**
- تا زمان ورود دفترچه رسمی ۱۴۰۵، هیچ اطلاعات ۱۴۰۵ حدس زده نمی‌شود.

## وقتی دفترچه ۱۴۰۵ منتشر شد
1. فایل‌ها و داده‌های رسمی دفترچه ۱۴۰۵ را در `data/selection_guides/1405/` قرار دهید.
2. در `data/selection_config.json` مقدار `active_analysis_year` را از `1404` به `1405` تغییر دهید.
3. ربات را دوباره اجرا کنید.

نیازی به تغییر کد Python نیست. اطلاعات کارنامه دانش‌آموزان حفظ می‌شود و فقط مرجع رشته‌محل‌ها، ظرفیت‌ها، ضوابط و شرایط انتخاب رشته عوض می‌شود.

## پیام شفاف برای کاربر
تا فعال شدن داده ۱۴۰۵، ربات باید صریحاً بگوید: «مبنای فعلی تحلیل دفترچه ۱۴۰۴ است و این تحلیل جایگزین دفترچه ۱۴۰۵ نیست.»
